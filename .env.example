# === me-imagemaxxing environment ===
# Copy to .env.local for local dev. In Railway, set these as service variables.

# SQLite database path (Railway: /data/app.db with volume mounted at /data)
DATABASE_PATH=./data/app.db

# OpenAI — required for Prompt mode with OpenAI provider
# Accepts either OPENAI_API_KEY or CHATGPT_API_KEY (Railway shared variable)
OPENAI_API_KEY=
# CHATGPT_API_KEY=

# Gemini — required for Prompt mode with Gemini/NanoBanana provider
GEMINI_API_KEY=

# Optional: override Gemini model (default: gemini-2.5-flash-image)
# GEMINI_MODEL=gemini-2.5-flash-image
