# === Required for auth ===
AUTH_SECRET=                    # Generate: openssl rand -base64 32
AUTH_DATABASE_URL="file:./auth.db"  # SQLite for auth (Prisma)
NEXTAUTH_URL=http://localhost:3000  # Your public URL (Railway sets this auto)
AUTH_TRUST_HOST=true               # Required for Railway/proxy deployments

# === Email magic links (optional — needed for email sign-in) ===
AUTH_RESEND_KEY=                # From resend.com dashboard
AUTH_EMAIL_FROM="Dating Profile Photomaxxing <noreply@resend.dev>"

# === Admin password bypass ===
ADMIN_PASSWORD=rrventures       # Password for admin sign-in

# === Image generation ===
GEMINI_API_KEY=                 # Google AI Studio API key
OPENAI_API_KEY=                 # OpenAI API key (or CHATGPT_API_KEY)

# === Optional overrides ===
# GEMINI_MODEL=gemini-3-pro-image-preview
# GEMINI_TEXT_MODEL=gemini-2.0-flash
# GEMINI_IMAGE_SIZE=1K
# OPENAI_CHAT_MODEL=gpt-4o-mini
